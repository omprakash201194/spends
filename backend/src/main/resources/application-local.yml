spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/homelab
    username: homelab
    password: ${DB_PASSWORD}
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true

server:
  port: 8080

logging:
  level:
    com.omprakashgautam: DEBUG
    org.springframework.security: DEBUG
