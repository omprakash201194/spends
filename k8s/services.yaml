apiVersion: v1
kind: Service
metadata:
  name: spends-backend
  namespace: homelab
spec:
  selector:
    app: spends-backend
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
---
apiVersion: v1
kind: Service
metadata:
  name: spends-frontend
  namespace: homelab
spec:
  selector:
    app: spends-frontend
  ports:
    - port: 80
      targetPort: 80
  type: ClusterIP
